Version: '2012-10-17'
Statement:
  - Effect: Allow
    Action:
      - secretsmanager:GetSecretValue
      - kms:Decrypt
      - ssm:GetParameter
      - ssm:GetParameters
    Resource:
      - "${kms_arn}"
      %{~ for arn in secrets ~}
      - "${arn}"
      %{~ endfor ~}
